// vite 不支持 antd 的 import { comP } from 'antd' 的写法

import {
    version,
    message,
    Button,
    Card,
    Checkbox,
    Dropdown,
    Form,
    Input,
    Radio,
    Table,
    Tooltip
} from 'antd'; // v4

export { version, message, Button, Card, Checkbox, Dropdown, Form, Input, Radio, Table, Tooltip };
